<h4><%= link_to image.title, '/images/'+image.id.to_s %></h4>
<%= link_to image_tag(image.img.url), '/images/'+image.id.to_s %><br/>
<%= link_to image.user.nickname, '/users/'+image.user.id.to_s %><br/>
<%= VoteImgsController.gettotal(image.id) %> pts<br/>
<% if (logged_in?) && (image.user.id == current_user.id) %>
<%= link_to "Irudia ezabatu", '/images/'+image.id.to_s+'/destroy' %><br/>
<% end %>
---------------------------------------------------------------------
